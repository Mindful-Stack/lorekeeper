---
name: lore-architect
description: Report which accepted architecture decisions bind a task, which decisions a codebase has made without a record, or where a diff contradicts a record.
---

# Lore architect

Read `../../references/codex-runtime.md`, resolve the knowledge paths, then follow
`../../agents/architect/AGENT.md` in the mode the caller named (`bind`, `survey`, or
`check`). Return the mode's report to the calling workflow; never write a record.
